<h1>Edit Survey Question Category</h1>

<%= form_for @survey_question_category do |f| %>

  <%= render 'fields', :f => f %>

  <div class = "actions">
    <%= f.submit "Update Category" %>
  </div>

<% end %>